# Runtime dependencies, kept in step with the extras in pyproject.toml.
#
# `pip install -e ".[dev,metrics,plots]"` (what `make install` runs) is the
# supported path; this file exists for environments that want a flat list.
#
# These are loose ranges for development. For any run whose numbers you intend
# to report, install from a lockfile instead and record the resolved versions
# (run_experiment writes them into metrics.json via utils/repro.py).
# transformers in particular has changed generation behaviour across releases
# within this range.

# Core
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0        # required by device_map="auto" in model_manager
pandas>=2.0.0
numpy>=1.24.0
pyyaml>=6.0
psutil>=5.9.0             # peak memory tracking

# Metrics. sentence-transformers backs OMA option mapping, semantic
# similarity, and query-conditioned playbook retrieval; without it retrieval
# silently degrades to retention-only ranking (the run warns when this happens).
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
sacrebleu>=2.3.1

# Plotting
matplotlib>=3.7.0
seaborn>=0.12.0

# Development
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.1.0

# Removed as unused (verified with grep across src/, scripts/ and tests/):
#   datasets, jsonlines, rouge-score, tqdm, evaluate
